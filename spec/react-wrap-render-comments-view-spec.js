'use babel';

import ReactWrapRenderCommentsView from '../lib/react-wrap-render-comments-view';

describe('ReactWrapRenderCommentsView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
